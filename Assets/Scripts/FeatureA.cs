using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FeatureA : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Im Feature A");   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
